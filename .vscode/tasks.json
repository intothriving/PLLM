{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "PLLM",
            "type": "shell",
            "command": "streamlit run main.py",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}